import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'gob.regionancash.gds.disabled',
  appName: 'disabled',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
